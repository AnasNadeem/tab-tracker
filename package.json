{
  "name": "tab-time-tracker",
  "version": "2.0.0",
  "description": "Track time spent on each tab and URL",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "archiver": "^7.0.1"
  }
}
